{
  "name": "stocks-monorepo",
  "private": true,
  "version": "1.0.0",
  "description": "Stocks management monorepo",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "pnpm -r build",
    "makeMigration": "pnpm --filter @stocks/database makeMigration",
    "migrate": "pnpm --filter @stocks/database migrate",
    "seed": "pnpm --filter @stocks/database seed"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^22.13.0"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
